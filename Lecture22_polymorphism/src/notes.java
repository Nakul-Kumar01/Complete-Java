public class notes {
/*
    polymorphism: many forms
    2 types:

    1) compile time(overloading) : decision is taken at compile time // Already Covered
    2) runtime: decision is taken at runtime
 */
}
